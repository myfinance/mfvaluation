apiVersion: v1
kind: Service
metadata:
 name: {{ .Values.mfvaluation.mf_name }}    
spec:
 #this defines which pod is going to be presented by this service
 selector:
  app: {{ .Values.mfvaluation.mf_name }}    
 ports:
  - name: http
    port: {{ .Values.mfvaluation.mf_http_port }}
    nodePort: {{ .Values.mfvaluation.mf_http_port_ext }}                 
 type: NodePort 
